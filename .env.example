# Copy this to .env, or set these directly as environment variables in
# whatever tool launches this connector (e.g. Claude Desktop's connector
# settings). Never commit a real .env file.

JDE_API_URL=https://your-service-name.onrender.com
JDE_API_KEY=paste-the-key-your-vendor-gave-you
